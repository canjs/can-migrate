import MyTarget from 'can/view/target/target';

const map = new MyTarget({
  define: {
    Type: MyTarget
  }
});
MyTarget();
new MyTarget();
MyTarget.prototype.say = function(){};
function speak(MyTarget) {};
const say = function(MyTarget) {};
Object.assign({}, MyTarget);
