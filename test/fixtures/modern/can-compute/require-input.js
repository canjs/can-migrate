const MyCompute = require('can/compute/compute');

const map = new MyCompute({
  define: {
    Type: MyCompute
  }
});
MyCompute();
new MyCompute();
MyCompute.prototype.say = function(){};
function speak(MyCompute) {};
const say = function(MyCompute) {};
Object.assign({}, MyCompute);
